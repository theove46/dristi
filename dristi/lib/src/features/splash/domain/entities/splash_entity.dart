import 'package:flutter/material.dart';

class SplashEntity {
  SplashEntity({
    required this.title,
    required this.image,
    required this.textColor,
  });

  String title;
  String image;
  Color textColor;
}
